import React from 'react';
import { Box } from '@mui/material';

// Placeholder for future CameraGrid component
const CameraGrid: React.FC = () => {
  return (
    <Box>
      {/* Camera grid will be extracted here in the future */}
    </Box>
  );
};

export default CameraGrid;