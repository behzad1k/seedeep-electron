import React from 'react';
import { Box } from '@mui/material';

// Placeholder for future Sidebar component
const Sidebar: React.FC = () => {
  return (
    <Box>
      {/* Sidebar will be extracted here in the future */}
    </Box>
  );
};

export default Sidebar;